apiVersion: batch/v1
kind: Job
metadata:
  name: ovpn-genconfig 
spec:
  template:
    spec:
      containers:
      - name: ovpn-genconfig 
        image: kylemanna/openvpn
        args:
          - "ovpn_genconfig"
          - "-u"
          - "udp://serg046.dev"
        volumeMounts:
        - name: ovpn-data-volume
          mountPath: "/etc/openvpn"
      volumes:
      - name: ovpn-data-volume
        hostPath:
          path: ovpn-data-volume
      restartPolicy: Never