apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openvpn-ingress
spec:
  rules:
  - 1194:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: openvpn-service
            port:
              number: 1194
